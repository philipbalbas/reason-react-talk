```reason
;
let addThree = add(3);
let divide = (~dividend, ~divisor) => dividend / divisor;

divide(~divident=10, ~divisor=2);
```

---

## Piping

**JavaScript**

```js
const friends = getFriends(getPerson(getId()));
```

